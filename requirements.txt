# Runtime dependencies for Streamlit Cloud deployment
# Pinned versions for reproducible builds and fast install times
# Dev/test dependencies are in pyproject.toml [project.optional-dependencies]

# Install the package itself (from repo root)
.

streamlit==1.38.0
pandas==2.2.2
numpy==1.26.4
plotly==5.24.0
requests==2.32.3
pyarrow==18.0.0
statsmodels==0.14.2
pillow==10.4.0
lxml==5.3.0
beautifulsoup4==4.12.3
